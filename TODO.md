# TODO for fixing signup issue

- [ ] Fix typo in Backend/src/model/User.js: change 'hasedPassword' to 'hashedPassword'
- [ ] Fix typo in Backend/src/model/session.js: change 'refeshToken' to 'refreshToken'
- [ ] Fix typos and logic in Backend/src/controller/authController.js: change 'hasedPassword' to 'hashedPassword', fix signIn response, add email check in signUp
- [ ] Test the signup endpoint
